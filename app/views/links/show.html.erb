<p>
  <strong>Url:</strong>
  <%= @link.url %>
</p>
 
<p>
  <strong>Slug:</strong>
  <%= @link.slug %>
</p>

<h2>Visits</h2>
<table class="table">
  <%= render @link.visits %>
</table>



<h2>Add a visit:</h2>
<%= render 'visits/form' %>

<%= link_to 'Edit Link', edit_link_path(@link) %> |
<%= link_to 'All Links', links_path %>